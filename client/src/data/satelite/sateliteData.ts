import { SateliteLocation } from "../../types/entities/sateliteLocation";
import { ref } from "vue";

export const locations = ref<SateliteLocation[]>();
export const locationsAggregated = ref<SateliteLocation[]>();
export const persistentParameters = ref<any>();
